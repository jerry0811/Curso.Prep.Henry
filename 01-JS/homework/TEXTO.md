# Explicacion de los aprendido

1.  Variables:

    basicamente podemos decir que una variable es:
    \*tipo de datos -> identificador variable -> valor almacenado

2.  Strings:

    se utiliza en cualquier lenguaje de programacion, se lo conoce tambien en español como "cadena" o "cadena de caracteres".

3.  Funciones:

    una función es una sección de un programa que calcula un valor de manera independiente al resto del programa, tambien tiene tres componentes importante.

    *Argumentos.- que son los valores que recibe como entrada.
    *Codigo.- que son las operaciones que tiene que realizar.
    \*return.- que es el valor que entrega la funcion.

4.  Declaraciones if:

    condicional if nos permite comprobar si se cumple una cierta condición con el fin de ejecutar un proceso u otro.

5.  Valores Booleanos:

    los tipos de datos booleanos solo tiene dos valores posible:
    verdadero y falso, se utiliza para condiciones.
